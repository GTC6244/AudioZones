{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GraphState",
  "description": "Everything a client needs to render, in one message.",
  "type": "object",
  "required": [
    "connected",
    "links",
    "nodes",
    "zones"
  ],
  "properties": {
    "connected": {
      "description": "True when the server is talking to a live PipeWire (false = degraded/mock).",
      "type": "boolean"
    },
    "links": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/LinkView"
      }
    },
    "nodes": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/NodeView"
      }
    },
    "zones": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ZoneView"
      }
    }
  },
  "definitions": {
    "Direction": {
      "type": "string",
      "enum": [
        "in",
        "out"
      ]
    },
    "LinkView": {
      "type": "object",
      "required": [
        "input_port",
        "output_port"
      ],
      "properties": {
        "input_port": {
          "type": "string"
        },
        "output_port": {
          "type": "string"
        }
      }
    },
    "NodeView": {
      "type": "object",
      "required": [
        "key",
        "media_class",
        "muted",
        "name",
        "ports",
        "present"
      ],
      "properties": {
        "channel_volumes": {
          "description": "Raw-linear per-channel volumes in the node's channel order (empty if no volume control). Lets a client show/drive individual channels (e.g. \"card ch 7-8 -> patio\").",
          "default": [],
          "type": "array",
          "items": {
            "type": "number",
            "format": "float"
          }
        },
        "key": {
          "description": "Stable key: `(node.name, media.class)[+serial]`. Survives reconnect; numeric PipeWire ids do not. See `identity`.",
          "type": "string"
        },
        "media_class": {
          "type": "string"
        },
        "muted": {
          "type": "boolean"
        },
        "name": {
          "type": "string"
        },
        "ports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PortView"
          }
        },
        "present": {
          "description": "False when the device is not currently present (unplugged); zones depending on it show \"degraded\".",
          "type": "boolean"
        },
        "volume": {
          "description": "Representative level, 0.0..=1.0 (the max across channels). `None` if this node has no volume control. For a one-knob UI; per-channel detail lives in `channel_volumes`.",
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        }
      }
    },
    "PortView": {
      "type": "object",
      "required": [
        "direction",
        "key",
        "name"
      ],
      "properties": {
        "direction": {
          "$ref": "#/definitions/Direction"
        },
        "key": {
          "description": "Stable key: `(node_key, port.name, direction)`.",
          "type": "string"
        },
        "name": {
          "type": "string"
        }
      }
    },
    "ZoneView": {
      "type": "object",
      "required": [
        "active",
        "degraded",
        "missing",
        "muted",
        "name"
      ],
      "properties": {
        "active": {
          "type": "boolean"
        },
        "degraded": {
          "description": "True when the zone is active but some of its devices/ports are missing.",
          "type": "boolean"
        },
        "missing": {
          "description": "Stable keys of devices the zone wants but can't currently reach.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "muted": {
          "description": "Live mute state of `volume_node` (false when there's no volume node).",
          "type": "boolean"
        },
        "name": {
          "type": "string"
        },
        "volume": {
          "description": "Live representative volume (0.0..=1.0) of `volume_node`, if that node is present. `None` -> the tile shows no slider (node absent or zone has no volume node).",
          "type": [
            "number",
            "null"
          ],
          "format": "float"
        },
        "volume_node": {
          "description": "The zone's representative node — the sink whose volume the zone tile controls (the first volume-spec node, else the sink behind the zone's first link). `None` when the zone has no controllable node. Clients PUT volume changes to this key.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    }
  }
}
